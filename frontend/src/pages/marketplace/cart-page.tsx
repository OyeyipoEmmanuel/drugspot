import { ArrowLeft, FileCheck2, Minus, Plus, ShoppingBag, Trash2 } from "lucide-react";
import { Link } from "react-router-dom";

import { VerifiedBadge } from "@/components/marketplace/verified-badge";
import { Button } from "@/components/ui/button";
import { formatNaira } from "@/lib/format";
import { useCart } from "@/providers/cart-provider";

export function CartPage() {
  const cart = useCart();
  if (!cart.items.length) return <div className="mx-auto max-w-2xl py-12 text-center"><div className="mx-auto grid size-20 place-items-center rounded-3xl bg-secondary text-primary"><ShoppingBag className="size-9" /></div><h1 className="mt-6 text-3xl font-bold">Your cart is empty</h1><p className="mt-2 text-muted-foreground">Search verified pharmacies and add the medicine you need.</p><Button asChild className="mt-6" size="lg"><Link to="/marketplace">Browse marketplace</Link></Button></div>;
  const deliveryFee = cart.pharmacy?.deliveryFee ?? 0;
  return <div className="space-y-6"><Button asChild variant="ghost" className="-ml-3"><Link to="/marketplace"><ArrowLeft />Continue shopping</Link></Button><section><p className="text-sm font-semibold text-primary">Your order</p><h1 className="mt-1 text-3xl font-bold">Cart</h1></section><div className="grid gap-6 lg:grid-cols-[1fr_360px]"><section className="space-y-4"><div className="rounded-2xl border bg-card p-4"><div className="flex flex-wrap items-center gap-2"><span className="font-bold">{cart.pharmacy?.name}</span><VerifiedBadge compact /></div><p className="mt-1 text-xs text-muted-foreground">Items in one order must come from the same pharmacy.</p></div>{cart.items.map((item) => <article key={item.offer.id} className="rounded-2xl border bg-card p-5 shadow-sm"><div className="flex gap-4"><div className="grid size-14 shrink-0 place-items-center rounded-2xl bg-secondary text-primary"><ShoppingBag /></div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-3"><div><h2 className="font-bold">{item.product.name}</h2><p className="text-sm text-muted-foreground">{item.product.strength} · {item.product.packSize}</p></div><Button variant="ghost" size="icon" onClick={() => cart.removeItem(item.offer.id)} aria-label={`Remove ${item.product.name}`}><Trash2 /></Button></div>{item.product.requiresPrescription && <p className="mt-3 flex items-center gap-1.5 text-xs font-semibold text-amber-700"><FileCheck2 className="size-4" />Prescription review required</p>}<div className="mt-4 flex items-center justify-between"><div className="flex items-center gap-2"><Button variant="outline" size="icon" onClick={() => cart.updateQuantity(item.offer.id, item.quantity - 1)}><Minus /></Button><span className="w-8 text-center font-bold">{item.quantity}</span><Button variant="outline" size="icon" onClick={() => cart.updateQuantity(item.offer.id, item.quantity + 1)}><Plus /></Button></div><p className="text-lg font-bold">{formatNaira(item.offer.price * item.quantity)}</p></div></div></div></article>)}</section><aside className="h-fit rounded-3xl border bg-card p-6 shadow-sm lg:sticky lg:top-24"><h2 className="text-xl font-bold">Order summary</h2><dl className="mt-5 space-y-3 text-sm"><div className="flex justify-between"><dt className="text-muted-foreground">Subtotal</dt><dd className="font-semibold">{formatNaira(cart.subtotal)}</dd></div><div className="flex justify-between"><dt className="text-muted-foreground">Delivery</dt><dd className="font-semibold">From {formatNaira(deliveryFee)}</dd></div><div className="flex justify-between border-t pt-4 text-base"><dt className="font-bold">Estimated total</dt><dd className="font-bold">{formatNaira(cart.subtotal + deliveryFee)}</dd></div></dl><Button asChild className="mt-6 w-full" size="lg"><Link to="/checkout">Continue to checkout</Link></Button><p className="mt-4 text-xs leading-5 text-muted-foreground">Final availability and prescription requirements are confirmed by the pharmacy before fulfilment.</p></aside></div></div>;
}
